import React from 'react';
import { MButton } from './components';

const App: React.FC = () => {
  return (
    <div className="app">
      <MButton>Submit</MButton>
    </div>
  );
};

export default App;
